#include<stdio.h>
#include<math.h>
int main(){
	int i,n,power;
	printf("please enter:");
	scanf("%d",&n);
	for(i=0;i<=n;i++){
	power=pow(3,i);
	printf("pow(3,%d)=%.2d\n",i,power);	
                   	}
    return 0;
         }
