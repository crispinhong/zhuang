#include<stdio.h>
int main(){
	int a[10]; 
	int i,n,sum;
	sum=0;
	double average;
	printf("please enter grade:");
	scanf("%d",&n);
	for(i=0;i<n;i++){
		scanf("%d",&a[i]);
		sum+=a[i];
	}
	average=sum*1.0/n;
	printf("%.2lf\n",average);
	for(i=0;i<n;i++){
	if(a[i]>average)
	printf("%d\n",a[i]);
} 
}
