#include<stdio.h>
int main(){
	int n,i,t,max;
	int s=0;
	int a[10];
	printf("please enter:");
	scanf("%d",&n);
	for(i=0;i<n;i++){
		scanf("%d",&a[i]);
		if(max<a[i])
		max=a[i];
		t=i+1;
	}
	printf("max=%d,%d",max,t);
}
